# Validation Ownership

The GitHub Actions result is authoritative because this workspace does not have
the Windows Flutter toolchain or the repository checkout. Source review and
lightweight delimiter checks were completed before publishing the branch.
