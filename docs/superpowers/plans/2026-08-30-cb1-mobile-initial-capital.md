# CB1 Management Mobile Initial-Capital Plan

1. Add backend contract tests for mobile aliases, server-derived summary,
   eligible cash accounts, exact policy flags, and action receipts.
2. Extend the existing FastAPI/repository read model only as required; reuse all
   existing protected database functions unchanged.
3. Add strict Flutter models and repository tests for list, evidence record,
   prepare, post, permission intersection, retry identities, and malformed
   payload rejection.
4. Add a compact Management initial-capital workspace and Financial Accounting
   launcher with explicit protected confirmation surfaces.
5. Run focused backend/Flutter tests, analyzer/formatter, full Flutter/backend
   suites, compile checks, and diff/worktree audits.
6. Publish a stacked Draft PR only after local verification; wait for exact-head
   CI, then update Master Issue #296 and Notion without completing broad CB1 or
   any human/release gate.
