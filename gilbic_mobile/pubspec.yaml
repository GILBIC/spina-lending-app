name: gilbic_mobile
description: Role-based mobile application for the SPINA lending platform.
publish_to: "none"
version: 0.4.0+4

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.3.1
  http: ^1.6.0
  package_info_plus: ^10.2.1
  path: ^1.9.1
  sqflite_sqlcipher: ^3.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
