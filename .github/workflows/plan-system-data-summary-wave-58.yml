name: Plan System Data summary Wave 58

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - "tools/plan_system_data_summary_wave58_tmp.py"
      - ".github/workflows/plan-system-data-summary-wave-58.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: plan-system-data-summary-wave-58-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    if: github.event_name != 'pull_request' || github.head_ref == 'agent/high-volume-wave-58'
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 30
    steps:
      - name: Check out exact PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          show-progress: false
      - name: Compile helper analyzer
        shell: cmd
        run: python -m py_compile tools\plan_system_data_summary_wave58_tmp.py
      - name: Analyze System Data helper boundaries
        shell: cmd
        run: python tools\plan_system_data_summary_wave58_tmp.py
      - name: Upload candidate report and sources
        uses: actions/upload-artifact@v4
        with:
          name: system-data-summary-wave-58-candidates
          path: |
            artifacts/wave-58-system-data-helper-candidates.json
            artifacts/wave-58-system-data-sources/*.py
          if-no-files-found: error
