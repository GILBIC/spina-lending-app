name: Inspect next date candidates

on:
  push:
    branches:
      - agent/next-utility-extraction

permissions:
  contents: write

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Generate candidate report
        run: python tools/inspect_next_date_candidates.py

      - name: Commit report when changed
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tools/fixtures/next_date_candidates.json
          if git diff --cached --quiet; then
            echo "Candidate report unchanged."
            exit 0
          fi
          git commit -m "Inspect next date candidates"
          git push origin HEAD:${GITHUB_REF_NAME}
