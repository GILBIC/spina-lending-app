name: Inspect log serialization helper

on:
  push:
    branches:
      - agent/extract-log-serialization-helper

permissions:
  contents: write

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Generate inspection report
        run: python tools/inspect_log_serialization_helper.py
      - name: Commit report
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tools/fixtures/log_serialization_helper_inspection.json
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Inspect log serialization helper"
          git push origin HEAD:${GITHUB_REF_NAME}
