name: 7x7 protected journal posting disposable PostgreSQL validation

on:
  pull_request:
    branches: [main]
    paths:
      - 'gilbic_backend/sql/0066_add_protected_7x7_source_event_journal_posting.sql'
      - 'gilbic_backend/tests/test_7x7_protected_journal_posting_postgres.py'
      - 'tools/run_7x7_protected_journal_posting_disposable_postgres_validation.py'
      - '.github/workflows/7x7-protected-journal-posting-disposable-postgres.yml'
  push:
    branches: [main]
    paths:
      - 'gilbic_backend/sql/0066_add_protected_7x7_source_event_journal_posting.sql'
      - 'gilbic_backend/tests/test_7x7_protected_journal_posting_postgres.py'
      - 'tools/run_7x7_protected_journal_posting_disposable_postgres_validation.py'
      - '.github/workflows/7x7-protected-journal-posting-disposable-postgres.yml'

permissions:
  contents: read

concurrency:
  group: seven-by-seven-journal-posting-disposable-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate-seven-by-seven-journal-posting:
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 60
    env:
      X7POST_PG_PORT: '55453'
      X7POST_PG_USER: x7post

    steps:
      - name: Check out exact revision
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Define isolated runner paths
        shell: powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File {0}
        run: |
          $ErrorActionPreference = 'Stop'
          $clusterBase = [System.IO.Path]::GetFullPath(
            (Join-Path $env:RUNNER_TEMP '..\spina-7x7-journal-posting-clusters')
          ).TrimEnd('\')
          $runRoot = Join-Path $clusterBase (
            'run-{0}-attempt-{1}' -f $env:GITHUB_RUN_ID, $env:GITHUB_RUN_ATTEMPT
          )
          $dockerName = 'spina-x7post-{0}-{1}' -f $env:GITHUB_RUN_ID, $env:GITHUB_RUN_ATTEMPT
          @(
            "X7POST_CLUSTER_BASE=$clusterBase",
            "X7POST_RUN_ROOT=$runRoot",
            "X7POST_PG_DATA=$(Join-Path $runRoot 'pgdata')",
            "X7POST_PG_LOG=$(Join-Path $runRoot 'postgres.log')",
            "X7POST_OWNERSHIP_MARKER=$(Join-Path $runRoot 'cluster-owned.marker')",
            "X7POST_READY_MARKER=$(Join-Path $runRoot 'cluster-ready.marker')",
            "X7POST_VENV=$(Join-Path $runRoot 'venv')",
            "X7POST_DOCKER_NAME=$dockerName"
          ) | ForEach-Object {
            $_ | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          }

      - name: Select isolated PostgreSQL runtime
        shell: powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File {0}
        run: |
          $ErrorActionPreference = 'Stop'
          $pgBin = $null
          $initdb = Get-Command initdb.exe -ErrorAction SilentlyContinue
          if ($initdb) { $pgBin = Split-Path -Parent $initdb.Source }
          if (-not $pgBin) {
            foreach ($root in @('C:\Program Files\PostgreSQL', 'C:\Program Files (x86)\PostgreSQL')) {
              if (-not (Test-Path $root)) { continue }
              $candidate = Get-ChildItem $root -Directory |
                ForEach-Object { Join-Path $_.FullName 'bin' } |
                Where-Object {
                  (Test-Path (Join-Path $_ 'initdb.exe')) -and
                  (Test-Path (Join-Path $_ 'pg_ctl.exe')) -and
                  (Test-Path (Join-Path $_ 'pg_isready.exe'))
                } | Select-Object -First 1
              if ($candidate) { $pgBin = $candidate; break }
            }
          }

          if ($pgBin) {
            "X7POST_MODE=local" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
            "X7POST_PG_BIN=$pgBin" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
            "X7POST_DATABASE_URL=postgresql://x7post@127.0.0.1:$env:X7POST_PG_PORT/postgres?sslmode=disable" |
              Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
            Write-Host "Using local PostgreSQL binaries at $pgBin."
            exit 0
          }

          $docker = Get-Command docker.exe -ErrorAction SilentlyContinue
          if (-not $docker) { $docker = Get-Command docker -ErrorAction SilentlyContinue }
          if (-not $docker) {
            throw 'This self-hosted runner has neither local PostgreSQL binaries nor Docker. Refusing to use any remote database.'
          }
          & $docker.Source version --format '{{.Server.Version}}'
          if ($LASTEXITCODE -ne 0) {
            throw 'Docker CLI exists but the Docker engine is not available.'
          }
          "X7POST_MODE=docker" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "X7POST_DOCKER_EXE=$($docker.Source)" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "X7POST_DATABASE_URL=postgresql://x7post:x7post_test_only@127.0.0.1:$env:X7POST_PG_PORT/postgres?sslmode=disable" |
            Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          Write-Host 'Using a temporary PostgreSQL 18 Docker container on loopback only.'

      - name: Initialize isolated temporary PostgreSQL
        shell: powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File {0}
        run: |
          $ErrorActionPreference = 'Stop'
          $probe = New-Object System.Net.Sockets.TcpClient
          try {
            $probe.Connect('127.0.0.1', [int]$env:X7POST_PG_PORT)
            throw "Reserved test port $env:X7POST_PG_PORT is already in use."
          }
          catch [System.Net.Sockets.SocketException] { }
          finally { $probe.Dispose() }

          if ($env:X7POST_MODE -eq 'local') {
            $pgCtl = Join-Path $env:X7POST_PG_BIN 'pg_ctl.exe'
            New-Item -ItemType Directory -Force -Path $env:X7POST_CLUSTER_BASE | Out-Null
            Get-ChildItem $env:X7POST_CLUSTER_BASE -Directory -Filter 'run-*-attempt-*' -ErrorAction SilentlyContinue |
              ForEach-Object {
                if ($_.FullName -eq $env:X7POST_RUN_ROOT) { return }
                $marker = Join-Path $_.FullName 'cluster-owned.marker'
                $pgData = Join-Path $_.FullName 'pgdata'
                if (-not (Test-Path $marker) -or -not (Test-Path $pgData)) { return }
                & $pgCtl -D $pgData status 2>$null
                if ($LASTEXITCODE -eq 0) {
                  & $pgCtl -D $pgData -m immediate -w stop
                  if ($LASTEXITCODE -ne 0) { throw "Failed to stop owned orphan $($_.FullName)." }
                }
                elseif ($LASTEXITCODE -ne 3) { throw "Could not verify owned orphan state at $($_.FullName)." }
                Remove-Item -Recurse -Force $_.FullName
              }
            if (Test-Path $env:X7POST_RUN_ROOT) { Remove-Item -Recurse -Force $env:X7POST_RUN_ROOT }
            New-Item -ItemType Directory -Force -Path $env:X7POST_RUN_ROOT | Out-Null
            $initdb = Join-Path $env:X7POST_PG_BIN 'initdb.exe'
            $pgReady = Join-Path $env:X7POST_PG_BIN 'pg_isready.exe'
            & $initdb -D $env:X7POST_PG_DATA -U $env:X7POST_PG_USER --auth=trust --encoding=UTF8 --no-locale
            if ($LASTEXITCODE -ne 0) { throw 'initdb failed.' }
            "run_id=$env:GITHUB_RUN_ID`npgdata=$env:X7POST_PG_DATA" |
              Set-Content -Path $env:X7POST_OWNERSHIP_MARKER -Encoding ascii
            & $pgCtl -D $env:X7POST_PG_DATA -l $env:X7POST_PG_LOG -o "-p $env:X7POST_PG_PORT -h 127.0.0.1" -w start
            if ($LASTEXITCODE -ne 0) { throw 'Temporary PostgreSQL cluster failed to start.' }
            & $pgReady -h 127.0.0.1 -p $env:X7POST_PG_PORT -U $env:X7POST_PG_USER -d postgres
            if ($LASTEXITCODE -ne 0) { throw 'Temporary PostgreSQL cluster is not ready.' }
          }
          elseif ($env:X7POST_MODE -eq 'docker') {
            & $env:X7POST_DOCKER_EXE rm -f $env:X7POST_DOCKER_NAME 2>$null | Out-Null
            & $env:X7POST_DOCKER_EXE run --detach --name $env:X7POST_DOCKER_NAME `
              -e POSTGRES_USER=x7post `
              -e POSTGRES_PASSWORD=x7post_test_only `
              -e POSTGRES_DB=postgres `
              -p "127.0.0.1:$env:X7POST_PG_PORT`:5432" postgres:18 | Out-Null
            if ($LASTEXITCODE -ne 0) { throw 'Temporary PostgreSQL Docker container failed to start.' }
            $ready = $false
            for ($attempt = 0; $attempt -lt 60; $attempt++) {
              & $env:X7POST_DOCKER_EXE exec $env:X7POST_DOCKER_NAME pg_isready -U x7post -d postgres *> $null
              if ($LASTEXITCODE -eq 0) { $ready = $true; break }
              Start-Sleep -Seconds 1
            }
            if (-not $ready) { throw 'Temporary PostgreSQL Docker container did not become ready.' }
          }
          else {
            throw 'No isolated PostgreSQL runtime mode was selected.'
          }
          'ready=true' | Set-Content -Path $env:X7POST_READY_MARKER -Encoding ascii

      - name: Create isolated verifier environment
        shell: cmd
        run: |
          if not exist "%X7POST_READY_MARKER%" exit /b 1
          python -m venv "%X7POST_VENV%" || exit /b 1
          "%X7POST_VENV%\Scripts\python.exe" -m pip install --disable-pip-version-check --upgrade pip || exit /b 1
          "%X7POST_VENV%\Scripts\python.exe" -m pip install --disable-pip-version-check "psycopg[binary]>=3.2,<4" "pytest>=8.3,<9" || exit /b 1

      - name: Compile protected verifier
        shell: cmd
        run: |
          "%X7POST_VENV%\Scripts\python.exe" -m py_compile tools\run_7x7_protected_journal_posting_disposable_postgres_validation.py || exit /b 1
          "%X7POST_VENV%\Scripts\python.exe" -m py_compile gilbic_backend\tests\test_7x7_protected_journal_posting_postgres.py || exit /b 1

      - name: Execute protected 7x7 journal-posting proof
        shell: cmd
        run: |
          "%X7POST_VENV%\Scripts\python.exe" tools\run_7x7_protected_journal_posting_disposable_postgres_validation.py --database-url-env X7POST_DATABASE_URL || exit /b 1

      - name: Stop and delete temporary PostgreSQL
        if: always()
        shell: powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File {0}
        run: |
          $ErrorActionPreference = 'Continue'
          if ($env:X7POST_MODE -eq 'local' -and $env:X7POST_PG_BIN -and (Test-Path $env:X7POST_PG_DATA)) {
            $pgCtl = Join-Path $env:X7POST_PG_BIN 'pg_ctl.exe'
            & $pgCtl -D $env:X7POST_PG_DATA status 2>$null
            if ($LASTEXITCODE -eq 0) {
              & $pgCtl -D $env:X7POST_PG_DATA -m fast -w stop
              if ($LASTEXITCODE -ne 0) { & $pgCtl -D $env:X7POST_PG_DATA -m immediate -w stop }
            }
            if (Test-Path $env:X7POST_RUN_ROOT) { Remove-Item -Recurse -Force $env:X7POST_RUN_ROOT }
          }
          elseif ($env:X7POST_MODE -eq 'docker' -and $env:X7POST_DOCKER_EXE) {
            & $env:X7POST_DOCKER_EXE rm -f $env:X7POST_DOCKER_NAME 2>$null | Out-Null
          }

      - name: Require a clean committed tree when Git metadata is available
        if: always()
        shell: powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File {0}
        run: |
          $ErrorActionPreference = 'Stop'
          $git = Get-Command git.exe -ErrorAction SilentlyContinue
          if ($git -and (Test-Path '.git')) {
            & $git.Source diff --check
            if ($LASTEXITCODE -ne 0) { throw 'git diff --check failed.' }
            & $git.Source diff --exit-code
            if ($LASTEXITCODE -ne 0) { throw 'Working tree changed during verification.' }
            & $git.Source diff --cached --exit-code
            if ($LASTEXITCODE -ne 0) { throw 'Index changed during verification.' }
          }
          else {
            Write-Host 'Git metadata is unavailable on this secondary self-hosted runner; actions/checkout exact-revision archive was used.'
          }
