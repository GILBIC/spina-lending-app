name: Apply display formatter extraction

on:
  push:
    branches:
      - agent/extract-display-formatters

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Check out extraction branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Compile guard and regression test
        run: |
          python -m py_compile \
            tools/extract_display_formatters.py \
            tools/test_display_formatter_extraction.py

      - name: Test original state and save behavior fixture
        run: |
          python tools/test_display_formatter_extraction.py \
            --write-fixture tools/fixtures/display_formatters_behavior.json

      - name: Run guarded dry-run
        run: |
          python tools/extract_display_formatters.py \
            --json /tmp/display-formatter-plan.json
          python - <<'PY'
          import json
          from pathlib import Path
          result = json.loads(Path('/tmp/display-formatter-plan.json').read_text())
          assert result['safe_to_apply'] is True
          assert result['targets']
          PY

      - name: Apply guarded extraction
        run: |
          python tools/extract_display_formatters.py \
            --apply \
            --json /tmp/display-formatter-result.json

      - name: Validate extracted state
        run: |
          python -m py_compile \
            OFFICIAL_SPINA_APP_PostgreSQL_TEST_v33_stability_performance_fixed.py \
            spina_app/utilities/formatting.py \
            tools/extract_display_formatters.py \
            tools/test_display_formatter_extraction.py
          python tools/test_display_formatter_extraction.py

      - name: Commit generated extraction
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            OFFICIAL_SPINA_APP_PostgreSQL_TEST_v33_stability_performance_fixed.py \
            spina_app/utilities/formatting.py \
            tools/fixtures/display_formatters_behavior.json
          if git diff --cached --quiet; then
            echo "No generated changes to commit."
            exit 0
          fi
          git commit -m "Extract display formatters"
          git push origin HEAD:${GITHUB_REF_NAME}
