name: Apply display UI helper batch 04

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/pure-display-helper-batch-04-work
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Validate exact original batch
        run: |
          python tools/extract_pure_helper_batch.py \
            --manifest tools/fixtures/display_ui_helper_batch_04_manifest.json
      - name: Capture original behavior
        run: |
          python tools/test_display_ui_helper_batch_04.py \
            --manifest tools/fixtures/display_ui_helper_batch_04_manifest.json \
            --fixture tools/fixtures/display_ui_helper_batch_04_behavior.json \
            --write-fixture
      - name: Apply guarded extraction
        run: |
          python tools/extract_pure_helper_batch.py \
            --manifest tools/fixtures/display_ui_helper_batch_04_manifest.json \
            --apply
      - name: Compile and compare behavior
        run: |
          python -m py_compile \
            OFFICIAL_SPINA_APP_PostgreSQL_TEST_v33_stability_performance_fixed.py \
            spina_app/utilities/formatting.py \
            spina_app/ui_helpers.py \
            tools/extract_pure_helper_batch.py \
            tools/test_display_ui_helper_batch_04.py
          python tools/test_display_ui_helper_batch_04.py \
            --manifest tools/fixtures/display_ui_helper_batch_04_manifest.json \
            --fixture tools/fixtures/display_ui_helper_batch_04_behavior.json
      - name: Commit extracted batch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            OFFICIAL_SPINA_APP_PostgreSQL_TEST_v33_stability_performance_fixed.py \
            spina_app/utilities/formatting.py \
            spina_app/ui_helpers.py \
            tools/fixtures/display_ui_helper_batch_04_behavior.json
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Extract display UI helper batch"
          git push origin HEAD:agent/pure-display-helper-batch-04-work
