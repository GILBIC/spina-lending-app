name: ECL credit-risk labels disposable PostgreSQL validation

on:
  pull_request:
    branches: [main]
    paths:
      - 'gilbic_backend/sql/0070_add_ecl_credit_risk_labels.sql'
      - 'gilbic_backend/sql/0071_harden_ecl_cash_recovery_chronology.sql'
      - 'gilbic_backend/sql/0072_add_ecl_quantitative_input_readiness.sql'
      - 'gilbic_backend/sql/0073_add_ecl_forward_looking_evidence_governance.sql'
      - 'gilbic_backend/sql/0074_integrate_ecl_forward_looking_readiness.sql'
      - 'gilbic_backend/sql/0075_add_read_only_quantitative_ecl_measurement.sql'
      - 'gilbic_backend/sql/0076_harden_read_only_quantitative_ecl_measurement.sql'
      - 'gilbic_backend/sql/0077_add_protected_ecl_allowance_posting.sql'
      - 'gilbic_backend/sql/0078_harden_ecl_allowance_posting_queue.sql'
      - 'gilbic_backend/src/gilbic_backend/ecl_credit_risk_label_repository.py'
      - 'gilbic_backend/src/gilbic_backend/ecl_credit_risk_label_api.py'
      - 'gilbic_backend/src/gilbic_backend/ecl_forward_looking_evidence_repository.py'
      - 'gilbic_backend/src/gilbic_backend/ecl_forward_looking_evidence_api.py'
      - 'gilbic_backend/src/gilbic_backend/ecl_quantitative_measurement_repository.py'
      - 'gilbic_backend/src/gilbic_backend/ecl_quantitative_measurement_api.py'
      - 'gilbic_backend/src/gilbic_backend/ecl_allowance_posting_repository.py'
      - 'gilbic_backend/src/gilbic_backend/ecl_allowance_posting_api.py'
      - 'gilbic_backend/tests/test_ecl_credit_risk_labels_migration.py'
      - 'gilbic_backend/tests/test_ecl_credit_risk_labels_postgres.py'
      - 'gilbic_backend/tests/test_ecl_cash_recovery_chronology_postgres.py'
      - 'gilbic_backend/tests/test_ecl_quantitative_input_readiness_postgres.py'
      - 'gilbic_backend/tests/test_ecl_forward_looking_evidence_postgres.py'
      - 'gilbic_backend/tests/test_ecl_read_only_measurement_migration.py'
      - 'gilbic_backend/tests/test_ecl_read_only_measurement_postgres.py'
      - 'gilbic_backend/tests/test_ecl_quantitative_measurement_api_contract.py'
      - 'gilbic_backend/tests/test_ecl_allowance_posting_migration.py'
      - 'gilbic_backend/tests/test_ecl_allowance_posting_postgres.py'
      - 'gilbic_backend/tests/test_ecl_allowance_posting_api_contract.py'
      - 'docs/accounting/ecl-read-only-measurement-policy.md'
      - 'docs/accounting/ecl-allowance-posting-policy.md'
      - 'tools/apply_ecl_credit_risk_labels_migration.py'
      - 'tools/run_ecl_credit_risk_labels_disposable_postgres_validation.py'
      - '.github/workflows/ecl-credit-risk-labels-disposable-postgres.yml'
  push:
    branches: [main]
    paths:
      - 'gilbic_backend/sql/0070_add_ecl_credit_risk_labels.sql'
      - 'gilbic_backend/sql/0071_harden_ecl_cash_recovery_chronology.sql'
      - 'gilbic_backend/sql/0072_add_ecl_quantitative_input_readiness.sql'
      - 'gilbic_backend/sql/0073_add_ecl_forward_looking_evidence_governance.sql'
      - 'gilbic_backend/sql/0074_integrate_ecl_forward_looking_readiness.sql'
      - 'gilbic_backend/sql/0075_add_read_only_quantitative_ecl_measurement.sql'
      - 'gilbic_backend/sql/0076_harden_read_only_quantitative_ecl_measurement.sql'
      - 'gilbic_backend/sql/0077_add_protected_ecl_allowance_posting.sql'
      - 'gilbic_backend/sql/0078_harden_ecl_allowance_posting_queue.sql'
      - 'gilbic_backend/src/gilbic_backend/ecl_credit_risk_label_repository.py'
      - 'gilbic_backend/src/gilbic_backend/ecl_credit_risk_label_api.py'
      - 'gilbic_backend/src/gilbic_backend/ecl_forward_looking_evidence_repository.py'
      - 'gilbic_backend/src/gilbic_backend/ecl_forward_looking_evidence_api.py'
      - 'gilbic_backend/src/gilbic_backend/ecl_quantitative_measurement_repository.py'
      - 'gilbic_backend/src/gilbic_backend/ecl_quantitative_measurement_api.py'
      - 'gilbic_backend/src/gilbic_backend/ecl_allowance_posting_repository.py'
      - 'gilbic_backend/src/gilbic_backend/ecl_allowance_posting_api.py'
      - 'gilbic_backend/tests/test_ecl_credit_risk_labels_migration.py'
      - 'gilbic_backend/tests/test_ecl_credit_risk_labels_postgres.py'
      - 'gilbic_backend/tests/test_ecl_cash_recovery_chronology_postgres.py'
      - 'gilbic_backend/tests/test_ecl_quantitative_input_readiness_postgres.py'
      - 'gilbic_backend/tests/test_ecl_forward_looking_evidence_postgres.py'
      - 'gilbic_backend/tests/test_ecl_read_only_measurement_migration.py'
      - 'gilbic_backend/tests/test_ecl_read_only_measurement_postgres.py'
      - 'gilbic_backend/tests/test_ecl_quantitative_measurement_api_contract.py'
      - 'gilbic_backend/tests/test_ecl_allowance_posting_migration.py'
      - 'gilbic_backend/tests/test_ecl_allowance_posting_postgres.py'
      - 'gilbic_backend/tests/test_ecl_allowance_posting_api_contract.py'
      - 'docs/accounting/ecl-read-only-measurement-policy.md'
      - 'docs/accounting/ecl-allowance-posting-policy.md'
      - 'tools/apply_ecl_credit_risk_labels_migration.py'
      - 'tools/run_ecl_credit_risk_labels_disposable_postgres_validation.py'
      - '.github/workflows/ecl-credit-risk-labels-disposable-postgres.yml'

permissions:
  contents: read

concurrency:
  group: ecl-credit-risk-labels-disposable-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate-ecl-credit-risk-labels:
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 60
    env:
      ECLLBL_PG_PORT: '55455'
      ECLLBL_PG_USER: ecllbl

    steps:
      - name: Check out exact revision
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Define isolated runner paths
        shell: powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File {0}
        run: |
          $ErrorActionPreference = 'Stop'
          $clusterBase = [System.IO.Path]::GetFullPath(
            (Join-Path $env:RUNNER_TEMP '..\spina-ecl-label-clusters')
          ).TrimEnd('\')
          $runRoot = Join-Path $clusterBase (
            'run-{0}-attempt-{1}' -f $env:GITHUB_RUN_ID, $env:GITHUB_RUN_ATTEMPT
          )
          $dockerName = 'spina-ecllbl-{0}-{1}' -f $env:GITHUB_RUN_ID, $env:GITHUB_RUN_ATTEMPT
          @(
            "ECLLBL_CLUSTER_BASE=$clusterBase",
            "ECLLBL_RUN_ROOT=$runRoot",
            "ECLLBL_PG_DATA=$(Join-Path $runRoot 'pgdata')",
            "ECLLBL_PG_LOG=$(Join-Path $runRoot 'postgres.log')",
            "ECLLBL_OWNERSHIP_MARKER=$(Join-Path $runRoot 'cluster-owned.marker')",
            "ECLLBL_READY_MARKER=$(Join-Path $runRoot 'cluster-ready.marker')",
            "ECLLBL_VENV=$(Join-Path $runRoot 'venv')",
            "ECLLBL_DOCKER_NAME=$dockerName"
          ) | ForEach-Object {
            $_ | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          }

      - name: Select isolated PostgreSQL runtime
        shell: powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File {0}
        run: |
          $ErrorActionPreference = 'Stop'
          $pgBin = $null
          $initdb = Get-Command initdb.exe -ErrorAction SilentlyContinue
          if ($initdb) { $pgBin = Split-Path -Parent $initdb.Source }
          if (-not $pgBin) {
            foreach ($root in @('C:\Program Files\PostgreSQL', 'C:\Program Files (x86)\PostgreSQL')) {
              if (-not (Test-Path $root)) { continue }
              $candidate = Get-ChildItem $root -Directory |
                ForEach-Object { Join-Path $_.FullName 'bin' } |
                Where-Object {
                  (Test-Path (Join-Path $_ 'initdb.exe')) -and
                  (Test-Path (Join-Path $_ 'pg_ctl.exe')) -and
                  (Test-Path (Join-Path $_ 'pg_isready.exe'))
                } | Select-Object -First 1
              if ($candidate) { $pgBin = $candidate; break }
            }
          }

          if ($pgBin) {
            "ECLLBL_MODE=local" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
            "ECLLBL_PG_BIN=$pgBin" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
            "ECLLBL_DATABASE_URL=postgresql://ecllbl@127.0.0.1:$env:ECLLBL_PG_PORT/postgres?sslmode=disable" |
              Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
            Write-Host "Using local PostgreSQL binaries at $pgBin."
            exit 0
          }

          $docker = Get-Command docker.exe -ErrorAction SilentlyContinue
          if (-not $docker) { $docker = Get-Command docker -ErrorAction SilentlyContinue }
          if (-not $docker) {
            throw 'This self-hosted runner has neither local PostgreSQL binaries nor Docker. Refusing to use any remote database.'
          }
          & $docker.Source version --format '{{.Server.Version}}'
          if ($LASTEXITCODE -ne 0) { throw 'Docker CLI exists but the Docker engine is not available.' }
          "ECLLBL_MODE=docker" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "ECLLBL_DOCKER_EXE=$($docker.Source)" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "ECLLBL_DATABASE_URL=postgresql://ecllbl:ecllbl_test_only@127.0.0.1:$env:ECLLBL_PG_PORT/postgres?sslmode=disable" |
            Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          Write-Host 'Using a temporary PostgreSQL 18 Docker container on loopback only.'

      - name: Initialize isolated temporary PostgreSQL
        shell: powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File {0}
        run: |
          $ErrorActionPreference = 'Stop'
          $probe = New-Object System.Net.Sockets.TcpClient
          try {
            $probe.Connect('127.0.0.1', [int]$env:ECLLBL_PG_PORT)
            throw "Reserved test port $env:ECLLBL_PG_PORT is already in use."
          }
          catch [System.Net.Sockets.SocketException] { }
          finally { $probe.Dispose() }

          if ($env:ECLLBL_MODE -eq 'local') {
            $pgCtl = Join-Path $env:ECLLBL_PG_BIN 'pg_ctl.exe'
            New-Item -ItemType Directory -Force -Path $env:ECLLBL_CLUSTER_BASE | Out-Null
            Get-ChildItem $env:ECLLBL_CLUSTER_BASE -Directory -Filter 'run-*-attempt-*' -ErrorAction SilentlyContinue |
              ForEach-Object {
                if ($_.FullName -eq $env:ECLLBL_RUN_ROOT) { return }
                $marker = Join-Path $_.FullName 'cluster-owned.marker'
                $pgData = Join-Path $_.FullName 'pgdata'
                if (-not (Test-Path $marker) -or -not (Test-Path $pgData)) { return }
                & $pgCtl -D $pgData status 2>$null
                if ($LASTEXITCODE -eq 0) {
                  & $pgCtl -D $pgData -m immediate -w stop
                  if ($LASTEXITCODE -ne 0) { throw "Failed to stop owned orphan $($_.FullName)." }
                }
                elseif ($LASTEXITCODE -ne 3) { throw "Could not verify owned orphan state at $($_.FullName)." }
                Remove-Item -Recurse -Force $_.FullName
              }
            if (Test-Path $env:ECLLBL_RUN_ROOT) { Remove-Item -Recurse -Force $env:ECLLBL_RUN_ROOT }
            New-Item -ItemType Directory -Force -Path $env:ECLLBL_RUN_ROOT | Out-Null
            $initdb = Join-Path $env:ECLLBL_PG_BIN 'initdb.exe'
            $pgReady = Join-Path $env:ECLLBL_PG_BIN 'pg_isready.exe'
            & $initdb -D $env:ECLLBL_PG_DATA -U $env:ECLLBL_PG_USER --auth=trust --encoding=UTF8 --no-locale
            if ($LASTEXITCODE -ne 0) { throw 'initdb failed.' }
            "run_id=$env:GITHUB_RUN_ID`npgdata=$env:ECLLBL_PG_DATA" |
              Set-Content -Path $env:ECLLBL_OWNERSHIP_MARKER -Encoding ascii
            & $pgCtl -D $env:ECLLBL_PG_DATA -l $env:ECLLBL_PG_LOG -o "-p $env:ECLLBL_PG_PORT -h 127.0.0.1" -w start
            if ($LASTEXITCODE -ne 0) { throw 'Temporary PostgreSQL cluster failed to start.' }
            & $pgReady -h 127.0.0.1 -p $env:ECLLBL_PG_PORT -U $env:ECLLBL_PG_USER -d postgres
            if ($LASTEXITCODE -ne 0) { throw 'Temporary PostgreSQL cluster is not ready.' }
          }
          elseif ($env:ECLLBL_MODE -eq 'docker') {
            & $env:ECLLBL_DOCKER_EXE rm -f $env:ECLLBL_DOCKER_NAME 2>$null | Out-Null
            & $env:ECLLBL_DOCKER_EXE run --detach --name $env:ECLLBL_DOCKER_NAME `
              -e POSTGRES_USER=ecllbl `
              -e POSTGRES_PASSWORD=ecllbl_test_only `
              -e POSTGRES_DB=postgres `
              -p "127.0.0.1:$env:ECLLBL_PG_PORT`:5432" postgres:18 | Out-Null
            if ($LASTEXITCODE -ne 0) { throw 'Temporary PostgreSQL Docker container failed to start.' }
            $ready = $false
            for ($attempt = 0; $attempt -lt 60; $attempt++) {
              & $env:ECLLBL_DOCKER_EXE exec $env:ECLLBL_DOCKER_NAME pg_isready -U ecllbl -d postgres *> $null
              if ($LASTEXITCODE -eq 0) { $ready = $true; break }
              Start-Sleep -Seconds 1
            }
            if (-not $ready) { throw 'Temporary PostgreSQL Docker container did not become ready.' }
          }
          else { throw 'No isolated PostgreSQL runtime mode was selected.' }
          'ready=true' | Set-Content -Path $env:ECLLBL_READY_MARKER -Encoding ascii

      - name: Create isolated verifier environment
        shell: cmd
        run: |
          if not exist "%ECLLBL_READY_MARKER%" exit /b 1
          python -m venv "%ECLLBL_VENV%" || exit /b 1
          "%ECLLBL_VENV%\Scripts\python.exe" -m pip install --disable-pip-version-check --upgrade pip || exit /b 1
          "%ECLLBL_VENV%\Scripts\python.exe" -m pip install --disable-pip-version-check "psycopg[binary]>=3.2,<4" "pytest>=8.3,<9" || exit /b 1

      - name: Compile ECL credit-risk label verifier
        shell: cmd
        run: |
          "%ECLLBL_VENV%\Scripts\python.exe" -m py_compile tools\run_ecl_credit_risk_labels_disposable_postgres_validation.py || exit /b 1
          "%ECLLBL_VENV%\Scripts\python.exe" -m py_compile gilbic_backend\tests\test_ecl_credit_risk_labels_postgres.py || exit /b 1
          "%ECLLBL_VENV%\Scripts\python.exe" -m py_compile gilbic_backend\tests\test_ecl_cash_recovery_chronology_postgres.py || exit /b 1
          "%ECLLBL_VENV%\Scripts\python.exe" -m py_compile gilbic_backend\tests\test_ecl_quantitative_input_readiness_postgres.py || exit /b 1
          "%ECLLBL_VENV%\Scripts\python.exe" -m py_compile gilbic_backend\tests\test_ecl_forward_looking_evidence_postgres.py || exit /b 1
          "%ECLLBL_VENV%\Scripts\python.exe" -m py_compile gilbic_backend\tests\test_ecl_read_only_measurement_postgres.py || exit /b 1
          "%ECLLBL_VENV%\Scripts\python.exe" -m py_compile gilbic_backend\tests\test_ecl_allowance_posting_postgres.py || exit /b 1

      - name: Execute ECL credit-risk label proof
        shell: cmd
        run: |
          "%ECLLBL_VENV%\Scripts\python.exe" tools\run_ecl_credit_risk_labels_disposable_postgres_validation.py --database-url-env ECLLBL_DATABASE_URL || exit /b 1

      - name: Stop and delete temporary PostgreSQL
        if: always()
        shell: powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File {0}
        run: |
          $ErrorActionPreference = 'Continue'
          if ($env:ECLLBL_MODE -eq 'local' -and $env:ECLLBL_PG_BIN -and (Test-Path $env:ECLLBL_PG_DATA)) {
            $pgCtl = Join-Path $env:ECLLBL_PG_BIN 'pg_ctl.exe'
            & $pgCtl -D $env:ECLLBL_PG_DATA status 2>$null
            if ($LASTEXITCODE -eq 0) {
              & $pgCtl -D $env:ECLLBL_PG_DATA -m fast -w stop
              if ($LASTEXITCODE -ne 0) { & $pgCtl -D $env:ECLLBL_PG_DATA -m immediate -w stop }
            }
            if (Test-Path $env:ECLLBL_RUN_ROOT) { Remove-Item -Recurse -Force $env:ECLLBL_RUN_ROOT }
          }
          elseif ($env:ECLLBL_MODE -eq 'docker' -and $env:ECLLBL_DOCKER_EXE) {
            & $env:ECLLBL_DOCKER_EXE rm -f $env:ECLLBL_DOCKER_NAME 2>$null | Out-Null
          }

      - name: Require a clean committed tree when Git metadata is available
        if: always()
        shell: powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File {0}
        run: |
          $ErrorActionPreference = 'Stop'
          $git = Get-Command git.exe -ErrorAction SilentlyContinue
          if ($git -and (Test-Path '.git')) {
            & $git.Source diff --check
            if ($LASTEXITCODE -ne 0) { throw 'git diff --check failed.' }
            & $git.Source diff --exit-code
            if ($LASTEXITCODE -ne 0) { throw 'Working tree changed during verification.' }
            & $git.Source diff --cached --exit-code
            if ($LASTEXITCODE -ne 0) { throw 'Index changed during verification.' }
          }
          else {
            Write-Host 'Git metadata is unavailable on this secondary self-hosted runner; actions/checkout exact-revision archive was used.'
          }
