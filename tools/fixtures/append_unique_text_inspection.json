{
  "target": "_append_unique_text",
  "line": 2864,
  "end_line": 2874,
  "signature": "existing: str, addition: str",
  "decorators": [],
  "uses_global_or_nonlocal": false,
  "external_names": [],
  "source": "def _append_unique_text(existing: str, addition: str) -> str:\n    e = (existing or \"\").strip()\n    a = (addition or \"\").strip()\n    if not a:\n        return e\n    if not e:\n        return a\n    # Avoid duplicate appends\n    if a in e:\n        return e\n    return e + \"\\n\" + a",
  "reference_count": 1,
  "references": [
    {
      "owner": "_merge_note_dict",
      "owner_type": "FunctionDef",
      "owner_line": 2878,
      "call_line": 2889,
      "call_source": "_append_unique_text(str(out.get(k) or \"\"), vv)"
    }
  ]
}
